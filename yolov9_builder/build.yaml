---
# build.yaml – nadpisuje BUILD_FROM wstrzykiwany przez HA Supervisor
# Wymuszamy Debian (glibc) zamiast Alpine (musl) bo PyTorch wymaga glibc
build_from:
  amd64: "ghcr.io/home-assistant/amd64-base-debian:latest"
  aarch64: "ghcr.io/home-assistant/aarch64-base-debian:latest"
